#pragma ocne

#include<bits/stdc++.h>
#include "Hasher.h"

using namespace std;

namespace ParallelAlgorithm {
    using mmmv = map<int, map<int, map<int, vector<int> > > >;
    mmmv g, rev_g;

    void GenerateGraph(const vector<vector<int> >& old_g) {
        for(const vector<int>& e : old_g) {
            
        }
    }
}